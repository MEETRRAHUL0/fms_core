@model IEnumerable<FMS_Core.Models.EFModels.TblItemUnits>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MeasuringUnits)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MeasuringUnits)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MeasurintUnitId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MeasurintUnitId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MeasurintUnitId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
